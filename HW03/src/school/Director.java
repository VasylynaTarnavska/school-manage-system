package school;

public class Director extends Teacher{

    public Director(String firstName, String lastName, int age, String subject, int salary) {
        super(firstName, lastName, age, subject, salary);
    }
}
